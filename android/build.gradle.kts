// Top-level build file. Plugin versions are declared here so module-level files
// can apply them without specifying a version each time.
plugins {
    id("com.android.application") version "8.2.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.22" apply false
}
